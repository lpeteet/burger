<!-- the home template, can take in data from express render function -->
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-6">
        <ul>
            {{#each burgers }}
                <li>
                    <a href= "/delete/{{id}}">
                        <input type="text" name="sandwich" value ="{{name}}" readonly>
                        <button class="btn btn-info" id="table-button">Devour It!</button>
                    </a>                
                    {{!-- <input type="text" name="sandwich" value =" {{name}}" readonly><button>Devour It!</button> --}}
                </li>
            {{/each}}
        </ul>
    </div>
    <div class="col-sm-6">
        <ul>
            {{#each eaten }}
                <li>
                    <input type="text" name="sandwich" value ="{{name}}" readonly>
                </li>
            {{/each}}
        </ul>

  </div>
</div>

<form action="/submitData" method="POST">
    <textarea type="text" name="burgername"></textarea>
    <br>
	<button class="btn btn-info" type="submit">Submit</button>
</form>

{{!--
    <a href= "/submitData" ><button class="btn btn-info" id="table-button">
        <i class="fa fa-table" aria-hidden="true"></i>Submit</button>
    </a>
--}}


